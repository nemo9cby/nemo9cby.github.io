lib/ contains dependency jar files except for JDT jar files. JDT jar files can be found under eclipse plugin.

src/ contaisn the source code, LogSmellDetector.java contains the main entry.

LogAntiPatternDetector.jar

Usage: java -jar LogAntiPatternDetector.jar <release_dir_path>